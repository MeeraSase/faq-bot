# Installation

### 1. Clone the repository
```bash
git clone https://github.com/MeeraSase/faq-bot.git
cd faq-bot

🛠 Installation & Setup
Requirements

Python version: 3.10+ recommended

Pip packages: All dependencies listed in requirements.txt

Flask

sentence-transformers

deep-translator

Other packages as per requirements.txt

Install Dependencies

Open your terminal/PowerShell.

Navigate to your project folder:

cd C:\Users\meera\OneDrive\Desktop\faq-bot


Install all required packages:

pip install -r requirements.txt


⚠ Make sure your Python version is compatible (>=3.10)

Run the Flask App

In the same terminal, start the app:

python app.py


Open your browser and go to:

http://127.0.0.1:5000


You should see the AI Multilingual FAQ Chatbot interface.
